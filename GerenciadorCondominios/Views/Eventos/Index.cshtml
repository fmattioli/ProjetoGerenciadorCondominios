@model IEnumerable<GerenciadorCondominios.BLL.Models.Evento>

@{
    ViewData["Title"] = "Eventos cadastrados";
    string controller = this.ViewContext.RouteData.Values["controller"].ToString();
}
<div class="tabelas">
    <partial name="_Confirmacoes" />
    <div class="collection with-header">
        <div class="collection-header grey darken-1">
            <div class="row">
                <div class="col s10">
                    <h6 class="white-text">Eventos cadastrados</h6>
                </div>
                <div class="col s2">
                    <a asp-controller="Eventos" asp-action="Create" class="waves-effect waves-light bnt-small btn-floating green darken-4 tooltip" data-position="right" data-tooltip="Novo evento">
                        <i class="material-icons">add</i>
                    </a>
                </div>
            </div>
        </div>
        <div class="collection-item">
            <table class="stripped highlight responsive">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Nome)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Data)


                        <th>Ações</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Nome)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Data)
                            </td>

                            <td>
                                <a asp-controller="Eventos" asp-action="Edit" asp-route-id="@item.EventoId" class="btn-floating btn-small blue darken-4 tooltip" data-position="right" data-tooltip="Atualizar evento">
                                    <i class="material-icons">mode_edit</i>
                                </a> |
                                <a onclick="ConfirmarExclusao(@item.EventoId, '@item.Nome')" class="btn-floating btn-small red darken-4 tooltip modal-trigger" data-position="right" data-target="modal" data-tooltip="Excluir evento">
                                    <i class="material-icons">delete_forever</i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>